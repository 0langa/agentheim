name: Architecture
on: [pull_request, push]
jobs:
  enforce:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.14' }
      - run: pip install pytest pytest-cov fastapi starlette uvicorn pydantic requests beautifulsoup4 playwright rich typer filelock platformdirs numpy
      - run: python scripts/roadmap-check.py --phase 6 --ci
      - run: pytest tests/ --cov --cov-fail-under=60 -q
